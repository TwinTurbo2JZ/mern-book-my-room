import React from "react";
import HotelPage from "./Hotelpage";

interface RoomsProps {
  room: any;
}
// { rooms }:

const Hotels = ({ room }: RoomsProps) => {
  return (
    <div>
      <h2>hotel</h2>
    </div>
  );
};

export default Hotels;
